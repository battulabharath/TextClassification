To run the code open the .ipynb file using Jupyter notebook or google colab.
The easy way of running the code and checking the output is using colab( if no Jupyter local env is available).
goto the link and 
after the notebook is loaded.
Click on => Runtime => Run All => Run Anyway